#!/usr/bin/env bash

install_homebrew() {
  if ! which brew > /dev/null 2>&1; then
    log_info "installing homebrew..."
    /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
  else
    log_info "found brew installation. skipping..."
  fi
}

install_homebrew
